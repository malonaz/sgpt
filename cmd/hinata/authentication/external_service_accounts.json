local service_accounts = [
  // Cli service account.
  {
    "id": "sgpt",
    "role_ids": ["chat:writer"],
  },
];


{
  service_accounts: [
    service_account + { type: "SERVICE_ACCOUNT_TYPE_API_KEY" }
    for service_account in service_accounts
  ]
}
